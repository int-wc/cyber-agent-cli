# AGENTS.md

## 1. 适用范围

- 本文件适用于仓库根目录及其全部子目录。
- 若某个子目录存在更近层级的 `AGENTS.md`，以更近层级文件为准。
- `README.md` 面向人类读者，`AGENTS.md` 面向 AI 智能体与自动化工具，负责提供可执行约束、协作边界与实现要求。

## 2. 规则优先级

1. 用户在当前任务中的明确要求。
2. 更近层级目录中的 `AGENTS.md`。
3. 本文件。
4. 仓库中现有代码、测试、配置、脚本与目录结构。
5. README、设计文档、接口文档等补充材料。
6. 通用最佳实践。

当上层规则与现有实现冲突时，优先回到项目中核对真实逻辑；若仍无法判断，保留框架并标记“需人工确认”。

## 3. 语言与注释规范

- 除代码、命令、路径、协议字段、库名、框架名、英文缩写与数字外，其余内容一律使用中文。
- 所有代码注释必须使用中文，重点解释设计原因、边界条件、异常分支、兼容性约束，不写空泛注释。
- 所有 `TODO`、`FIXME`、占位说明、设计备注、变更说明、交付说明均使用中文。
- 标识符命名遵循语言与框架惯例，允许使用英文；禁止使用拼音命名，禁止中英混杂的变量名。
- 用户可见文案默认使用中文；若项目已有国际化方案，则遵循现有语言体系，并在注释中用中文说明原因。

## 4. 核心工作原则

- 先理解，后修改：先阅读相关代码、配置、测试与调用链，再开始实现。
- 先复用，后新增：优先复用项目已有模块、工具函数、类型定义、样式方案、错误处理与目录分层。
- 先对齐，后扩展：新增代码必须与现有项目的命名、分层、接口风格、异常语义保持一致。
- 小步交付，可验证：每次改动应尽量保持范围可控、影响可追踪、结果可验证。
- 假设显式化：凡是无法从项目中确认的信息，必须明确标注假设，不得悄悄补全为“看起来合理”的实现。

## 5. 逻辑回归要求

- 任何新增设计都必须先回到项目中寻找对应的逻辑来源，包括但不限于：
  - 同类模块的实现方式。
  - 现有数据模型、类型定义与字段命名。
  - 现有控制器、服务层、仓储层或状态管理逻辑。
  - 现有测试、脚本、文档与错误处理方式。
- 新增业务逻辑前，至少完成以下检查：
  1. 找入口：谁调用这个能力，输入从哪里来。
  2. 找旧逻辑：项目里是否已有相同或相近实现。
  3. 找数据：字段、类型、状态、枚举是否已有定义。
  4. 找约束：权限、事务、异常、日志、缓存是否已有既定处理方式。
  5. 找出口：返回值、落库结构、事件、副作用会影响哪些位置。
- 若项目中已经存在对应逻辑，必须优先沿用其设计，不得仅凭通用经验重造一套“更理想”的实现。
- 若项目中不存在可参考逻辑，只能生成最小可运行骨架，不得虚构业务规则、状态流转、接口语义或数据库含义。

## 6. 代码生成策略

- 默认生成“框架代码”，而不是一次性补满全部业务细节。
- 所有新增模块都应显式保留人工补写空间，方便后续手动完善，以提升项目成员的代码编写能力。
- 生成框架代码时，优先保留以下结构：
  - 明确的输入输出类型。
  - 清晰的模块边界与职责拆分。
  - 可替换的扩展点、策略点、适配层或接口层。
  - 必要但最小化的错误处理骨架。
  - 可继续补写的业务占位区。
- 需要人工补写的部分，统一使用中文占位标记，例如：

```ts
// TODO(人工实现): 在此补充核心业务逻辑
// TODO(业务确认): 此处字段含义需结合现有模块再确认
// TODO(联调补全): 待接口联调后补充异常分支
```

- 严禁为了“显得完整”而硬写大段猜测性业务代码、伪造枚举值、伪造接口字段、伪造数据库语义。
- 若仅能确认技术框架，不能确认业务细节，则应输出骨架、接口、占位与约束说明，而不是臆造完整实现。

## 7. 设计与实现要求

- 严谨与逻辑自洽优先于“快速产出”。
- 同一概念在项目中应保持同一命名，不得在不同模块中创造多个近义名称。
- 同类问题优先使用同类解法，避免同一仓库出现多套风格冲突的实现。
- 新增抽象必须有实际收益；若不能明显降低耦合或提升复用性，则保持简单实现。
- 公共接口、核心流程、复杂分支应补充简洁中文注释，说明“为什么这样设计”，而不是重复代码字面含义。
- 避免魔法值、重复逻辑与隐式副作用；出现重复时优先检查仓库中是否已有公共实现可复用。

## 8. 需要先确认的操作

- 新增、删除、升级依赖。
- 修改数据库结构、迁移脚本、初始化脚本。
- 修改认证、鉴权、加密、支付、计费、风控等高风险逻辑。
- 修改公共接口契约、事件结构、对外暴露的 SDK 或共享类型。
- 删除文件、重命名核心模块、批量搬迁目录。
- 修改 CI/CD、构建脚本、发布流程、环境变量模板。

如任务上下文已明确允许上述操作，则按用户要求执行；否则先给出影响范围与原因，再等待确认。

## 9. 禁止行为

- 禁止脱离项目现状凭空设计业务规则。
- 禁止为了通过编译或测试而删除校验、删除异常处理、放宽类型约束、篡改测试预期。
- 禁止把密钥、令牌、账号、证书、私有地址等敏感信息硬编码到仓库中。
- 禁止在无明确依据时重写核心模块、全局配置或基础设施代码。
- 禁止进行与当前任务无关的大范围重构或顺手优化。
- 禁止生成与现有架构明显冲突的代码风格、目录结构或分层模型。

## 10. 交付要求

- 交付说明使用中文，需明确说明本次改动做了什么、依据了哪些现有逻辑、还有哪些部分需要人工补写。
- 若存在假设、占位、待联调项、待业务确认项，必须显式列出，不得省略。
- 若进行了验证，说明验证方式；若未验证，也需明确说明未验证原因。
- 只要新增的是框架代码，就应让后续开发者能直接看懂哪些地方可以继续写、应该继续写什么、为什么目前不继续写。
- 每次任务完成后生成 git 提交，提交信息必须使用中文；如有条件可推送，即推送。

## 11. 推荐执行清单

在开始实现前，默认按以下顺序工作：

1. 阅读当前任务相关文件。
2. 查找同类实现与调用链。
3. 提炼现有命名、分层、类型与异常处理方式。
4. 基于现有逻辑搭建最小框架。
5. 用中文注释标出人工补写区与风险点。
6. 完成后检查是否与项目现有风格和逻辑一致。

本文件的目标不是让 AI 自由发挥到覆盖一切，而是让 AI 在项目既有逻辑之内稳定协作、严谨落地、为人工继续编写代码留下清晰空间。
